# Auto-detect text files and normalize line endings
* text=auto

# Shell scripts must use LF (they run inside Linux containers)
*.sh text eol=lf

# Dockerfile and related files use LF
Dockerfile text eol=lf
docker-compose*.yml text eol=lf
